#include <ros/ros.h>

int main(int argc, char** argv){
  ros::init(argc, argv, "tf");
  ros::NodeHandle node;

  ros::Rate rate(10.0);
  while (node.ok()){
    std::cout<<"fine"<<"\n";
    rate.sleep();
  }
  return 0;
}
